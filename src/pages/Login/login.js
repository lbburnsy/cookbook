import React from "react";
import LoginForm from "../../components/LoginForm/loginForm"

function Login() {
    return(
        <div className= "Login">
        <LoginForm/>
        </div>
    );
}

export default Login